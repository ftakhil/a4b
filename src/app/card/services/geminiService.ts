export const getMonolithResponse = async (text: string, history: any[]) => {
    // Stub implementation
    await new Promise(resolve => setTimeout(resolve, 1000));
    return "I am Monolith. Connect me to the real API.";
};
